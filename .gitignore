# BEADS runtime
.beads/beads.db
.beads/daemon.lock
.beads/daemon.pid
.beads/daemon.log
.beads/temp/

# BEADS execution state (working files for context recovery, not source code)
.beads/context/
.beads/plans/

# Secrets
.env
.env.*
!.env.example

# Node
node_modules/

# Pack-system v0 build + code-coverage outputs (WU0).
# `coverage-contract/` is intentionally NOT ignored — it holds a checked-in
# README placeholder until WU8 lands the custom contract reporter.
dist/
coverage/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
